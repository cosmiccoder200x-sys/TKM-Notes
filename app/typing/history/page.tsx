import HistoryView from "@/components/typing/HistoryView";

export const metadata = {
  title: "Typing History — TKM Notes",
  description: "Every typing test you have taken, with filters and your local record.",
};

export default function TypingHistoryPage() {
  return <HistoryView />;
}
